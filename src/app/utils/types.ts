// eslint-disable-next-line @typescript-eslint/no-unused-vars
type User = {
    id?:string; 
    name: string;
    username: string;
    email: string;
}
type LoginFormInputs={
    email:string,
    password:string
}